"$schema" = 'https://starship.rs/config-schema.json'

format = """
[](#fa824c)\
$directory\
[](fg:#fa824c bg:#716969)\
$git_branch\
$git_status\
[](fg:#716969 bg:#2d2e2e)\
$c\
$elixir\
$elm\
$golang\
$gradle\
$haskell\
$java\
$julia\
$nodejs\
$bun\
$python\
$nim\
$rust\
$scala\
$nix_shell\
[](fg:#2d2e2e bg:#0f0f0f)\
$docker_context\
[ ](fg:#0f0f0f)\
\n$character"""

add_newline = true

[character]
success_symbol = "[❯](fg:#fa824c)"
# error_symbol = "[](fg:#fa824c)"

# You can also replace your username with a neat symbol like   or disable this
# and use the os module below
[username]
show_always = true
style_user = "bg:#716969"
style_root = "bg:#716969"
format = '[$user ]($style)'
disabled = false

# An alternative to the username module which displays a symbol that
# represents the current operating system
[directory]
style = "bg:#fa824c"
format = "[$path ]($style)"
truncation_length = 3
truncation_symbol = "…/"

# Here is how you can shorten some long paths by text replacement
# similar to mapped_locations in Oh My Posh:
[directory.substitutions]
"Documents" = "󰈙 "
"Downloads" = " "
"Music" = " "
"repos" = " "
"Pictures" = " "
# Keep in mind that the order matters. For example:
# "Important Documents" = " 󰈙 "
# will not be replaced, because "Documents" was already substituted before.
# So either put "Important Documents" before "Documents" or use the substituted version:
# "Important 󰈙 " = " 󰈙 "

[c]
symbol = " "
style = "bg:#2d2e2e"
format = '[ $symbol ($version) ]($style)'

[docker_context]
symbol = " "
style = "bg:#0f0f0f"
format = '[ $symbol $context ]($style)'

[elixir]
symbol = " "
style = "bg:#2d2e2e"
format = '[ $symbol ($version) ]($style)'

[elm]
symbol = " "
style = "bg:#2d2e2e"
format = '[ $symbol ($version) ]($style)'

[git_branch]
symbol = ""
style = "bg:#716969"
format = '[ $symbol $branch ]($style)'

[git_status]
style = "bg:#716969"
format = '[$all_status$ahead_behind ]($style)'

[golang]
symbol = " "
style = "bg:#2d2e2e"
format = '[ $symbol ($version) ]($style)'

[gradle]
style = "bg:#2d2e2e"
format = '[ $symbol ($version) ]($style)'

[haskell]
symbol = " "
style = "bg:#2d2e2e"
format = '[ $symbol ($version) ]($style)'

[java]
symbol = " "
style = "bg:#2d2e2e"
format = '[ $symbol ($version) ]($style)'

[julia]
symbol = " "
style = "bg:#2d2e2e"
format = '[ $symbol ($version) ]($style)'

[nodejs]
symbol = ""
style = "bg:#2d2e2e"
format = '[ $symbol ]($style)'

[bun]
style = "bg:#2d2e2e"
symbol = "🚀 "
format = '[ $symbol($version) ]($style)'

[python]
style = "bg:#2d2e2e"
format = '[ $symbol($version) ]($style)'

[nim]
symbol = "󰆥 "
style = "bg:#2d2e2e"
format = '[ $symbol ($version) ]($style)'

[rust]
symbol = ""
style = "bg:#2d2e2e"
format = '[ $symbol ($version) ]($style)'

[scala]
symbol = " "
style = "bg:#2d2e2e"
format = '[ $symbol ($version) ]($style)'

[nix_shell]
symbol = "󱄅"
style = "bg:#2d2e2e"
format = '[$symbol $name ]($style)'

[time]
disabled = false
time_format = "%R"              # Hour:Minute Format
style = "bg:#fbfbfb fg:#2d2e2e"
format = '[ $time ]($style)'
